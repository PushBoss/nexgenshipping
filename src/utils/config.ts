// Configuration for data persistence
export const config = {
  // Set to true to use Supabase backend for persistent data storage
  // Set to false to use local state only (prototype mode)
  useSupabase: true,
  
  // Enable this to see detailed logs for API calls
  debugMode: false,
};
